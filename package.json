{
  "name": "ruszdup",
  "version": "1.0.0",
  "description": "Rusz Dupę - Travel planning application",
  "private": true,
  "scripts": {
    "setup": "docker compose build && docker compose -f docker-compose.test.yml run --rm composer && cd frontend && npm ci",
    "test:backend": "docker compose -f docker-compose.test.yml run --rm backend-test",
    "test:unit": "cd frontend && npm run test:unit",
    "test:e2e": "npx playwright test",
    "test:e2e:ui": "npx playwright test --ui",
    "test:all": "npm run test:backend && npm run test:unit && npm run test:e2e",
    "dev": "docker compose up -d",
    "dev:logs": "docker compose logs -f",
    "dev:down": "docker compose down",
    "clean": "docker compose down -v --remove-orphans && docker compose -f docker-compose.test.yml down -v --remove-orphans"
  },
  "dependencies": {
    "axios": "^1.12.2",
    "pinia": "^3.0.3",
    "tailwindcss": "^4.1.14",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
